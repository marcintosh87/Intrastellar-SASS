<%= form_with(model: e_comment) do |form| %>
  <% if e_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(e_comment.errors.count, "error") %> prohibited this e_comment from being saved:</h2>

      <ul>
        <% e_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :event_post_id %>
    <%= form.text_field :event_post_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
